#include <stdio.h>
#include "main.h"
/**
 * add - addition
 * @n: first oper
 * @l: second operator
 * Return: 1 if the character is lowercase, 0 otherwise
 */
int add(int n, int l)
{
	return (n + l);
}
